# 技术名词表（个人技术栈梳理）

> 结合： NPU 算子开发+ 推理部署）+ ONNX Runtime/TVM/MNN/MLIR 学习线


---

## 一、T41 时期：硬件 / 体系结构层

| 你当时叫它 | 正式术语 | 一句话解释 |
|---|---|---|
| 写 `NNMAC/LD/LU` 宏 | ✅ **NPU ISA 编程** | 直接在指令集层面写算子，宏是汇编助记符的封装 |
| `LD(VR0, 0, ptr, 0)` | ✅ **向量寄存器 + 地址寻址** | Load 指令：目标向量寄存器、偏移、基址、步长 |
| FRAM/WRAM 放不下数据 | ✅ **片上存储（on-chip SRAM）管理** | 只有几百 KB 的片上内存，要精打细算布局 |
| 手动规划地址 | ✅ **寄存器分配 / 内存分配（register allocation / memory planning）** | 你手工做的，编译器该自动做的事 |
| 访问同一 bank 变慢 | ✅ **Bank Conflict（bank 冲突）** | 多个访问落在同一 bank 被硬件串行化，本该并行 |
| 读写互相等 | ✅ **RAW Hazard（读后写冲突）** | 后一条指令读的数据前一条刚写，流水线必须停顿 |
| 循环里嵌套不必要的 if | ✅ **分支发散（Divergence）+ 循环不变代码外提（LICM）** | SIMD/MAC 阵列锁步执行，if 让部分 lane 空转 |
| 索引反复重算 | ✅ **公共子表达式消除（CSE）+ 地址计算优化（address computation optimization）** | 每次循环重算 `i*stride+offset`，应提出循环 |
| 数据放不下，切块算 | ✅ **Loop Tiling（循环分块）** | 切成 NNMAC 吃得了的小块，分多次算 |
| 决定先搬哪块、后算哪块 | ✅ **指令调度（instruction scheduling）** | 排指令顺序，让计算和搬运错开 |
| 量化/反量化 | ✅ **INT8/FP16 量化与反量化** | 数据类型转换，NPU 只有整数 MAC |

## 二、T41 时期：数据搬运侧优化（护城河）

| 你当时叫它 | 正式术语 | 一句话解释 |
|---|---|---|
| DMA pingpong | ✅ **双缓冲（Double Buffering）/ 软件流水（Software Pipelining）** | 搬下一块数据的同时算当前块，隐藏搬运延迟 |
| DMA rollback | ✅ **传输回滚 / 搬运调度失败补偿** | DMA 与计算重叠失败时的代价，规划"何时发搬运" |
| DMA full | ✅ **带宽瓶颈 / Roofline 模型** | DMA 带宽打满，MAC 阵列等数据，利用率上不去 |
| 手工规划搬运节奏 | ✅ **内存层次（Memory Hierarchy）管理** | DDR → 片上 → 寄存器的数据流规划 |
| 切 NNMAC 能吃的块 | ✅ **数据复用（data reuse）+ tiling** | 减少重复搬运，让数据在片上多算几次 |

> 关键认知：**前 4 个（bank/依赖/if/索引）是计算侧问题，后 3 个（pingpong/rollback/full）是搬运侧问题**。NPU 算子性能 = 计算效率 × 搬运效率。

## 三、T41 时期：两条写算子路径 = 人肉编译器

| 你的做法 | 编译器里的对应 | 本质 |
|---|---|---|
| 宏 + 脚本翻译成 asm | ✅ **Codegen（代码生成器）** | 你的脚本 = 一个手写的 IR → 汇编 lowering pass |
| intrinsics 直接编译 | ✅ **编译器 intrinsics 层** | 编译器帮你做寄存器分配/指令调度，但 tiling/双缓冲还要自己来 |
| 宏 ≈ 高级指令，不关心寄存器 | ✅ **中间表示（IR）** | 一句话"高级指令"，底层细节由后端填充 |
| 手动调 MAC 利用率 | ✅ **Roofline 分析 / 峰值利用率** | 验证计算单元有没有吃饱 |
| 工具链用了 mips/riscv | ✅ **公开指令集 + 自研脚本前端** | 宏 → 汇编（厂商自研脚本）→ `.o`（as）→ 链接（ld） |

## 四、T41 时期：新增回忆的技术

| 你回忆的叫法 | 正式术语 | 一句话解释 |
|---|---|---|
| **可编程 DMA** | ✅ **Descriptor-based DMA / 2D-DMA / 链式 DMA（Scatter-Gather DMA）** | DMA 不是"搬一块"，而是读一张描述符链表，自动连续搬多块、支持二维 stride 搬移、搬完自动触发中断/下一跳——你配描述符 = 在写搬运程序 |
| **DW 权重压缩** | ✅ **Depthwise 权重重排 / 权重打包（weight repacking）** | Depthwise 卷积权重通道独立、尺寸极小，按 NPU 的 MAC 阵列组织方式重排打包（如 4x4 分块、通道补齐），提高搬运和计算的利用率 |
| **DMA 压缩** | ✅ **数据压缩搬运（compressed DMA transfer）** | 对权重/稀疏数据先压缩再搬运，片上/总线带宽不变的情况下"多搬"数据，本质是**用计算换带宽** |
| **stride=2 时 kernel 拆分** | ✅ **空间下采样分解 / 输入分裂（kernel splitting for stride-2）** | stride=2 的卷积等价于把输入按奇偶坐标拆成 4 个子图，分别做 stride=1 卷积再拼——拆完的卷积都是**标准稠密计算**，能吃到 MAC 阵列满利用率 |
| **softmax 查表：3bit 表 + 5bit 插值** | ✅ **分段线性 LUT 近似（piecewise-linear LUT）+ 幂分解（exponent decomposition）** | exp 无 FPU 算不起 → 换底成 `2^x` 只查 [0,1)：整数部分用移位还原 `2^n`；小数部分 8bit 定点，高 3bit 定 8 段、低 5bit 做 32 级线性插值，只存 9 个端点表项，全程整数乘加无分支 |

> 搬运侧优化全家桶：**pingpong（双缓冲）→ rollback（回滚）→ full（带宽打满）→ 可编程 DMA（描述符链）→ DMA 压缩（带宽倍增）**

## 五、T41 时期：推测做过但没提的（需核对）

| 类别 | 术语 | 大概率遇到过的场景 |
|---|---|---|
| **计算侧** | 🧠 **循环展开（Loop Unrolling）** | 手工把内层循环展开 4 份，减少循环开销、增加指令级并行 |
| | 🧠 **数据复用（Data Reuse）** | 同一块 feature 在片上多算几次再写回，减少 DDR 往返 |
| | 🧠 **权重布局变换（Weight Layout Transform）** | NHWC→NCHW、通道重排，让 DMA 搬运连续、MAC 读起来顺 |
| | 🧠 **im2col / 直接卷积** | 卷积要么展开成矩阵乘（im2col 吃内存），要么手写直接卷积（省内存费功夫） |
| | 🧠 **Pad 与边界处理（Padding / Edge Handling）** | 卷积边界补零，跟"循环内 if"是一对难兄难弟 |
| | 🧠 **SIMD 向量化（Vectorization）** | 向量寄存器一次算 4/8 个元素——`LD(VR0,...)` 那个 VR 就是干这个的 |
| **存储侧** | 🧠 **内存对齐（Memory Alignment）** | 地址按 16/32 字节对齐，DMA 才高效、bank 才不串行 |
| | 🧠 **片上存储复用（Buffer Reuse）** | 前一层输出占的空间，后一层直接复用——手动做的"bufferization" |
| | 🧠 **混合精度（Mixed Precision）** | INT8 vs FP16 vs FP32 的取舍 |
| **调度侧** | 🧠 **指令级并行（ILP）** | 让 NNMAC 和 LD/ST 交错，计算和搬运同时进行 |
| | 🧠 **软件流水（Software Pipelining）** | 这是 pingpong 的理论名字 |
| | 🧠 **内核融合（Kernel Fusion）** | Conv+BN+ReLU 合成一个算子，少一次片上↔DDR 往返 |
| | 🧠 **多核/多通道并行（Parallelism）** | 多个 MAC 阵列或通道同时算，摊分同步开销 |

## 六、现在：推理部署层

| 场景 | 术语 |
|---|---|
| ECNR：ONNX → QNN 编译 → Hexagon NPU | ✅ **QNN 工具链 / Hexagon HTP（v73）**，`003_fp16_ctx.bin` = fp16 量化模型 |
| ECNR 的 14 个 cache 状态 | ✅ **状态流式模型（streaming）/ 自回归状态回灌** |
| KWS：ARM Cortex-A7 优化 | ✅ **NEON 向量化 + OpenMP 并行**（group_conv2d/Gemm 并行化） |
| 开放词唤醒（aivox_ovkws） | ✅ **开放词 KWS 工程**：登上 **2026 世界人工智能大会（WAIC）**，被央视直播展示 |
| 量化到定点 | ✅ **定标（scale → multiplier + shift）**，避免浮点计算 |
| fast20k on GX8006 SNPU | ✅ **SNPU 任务封装**：`model.h` 的 cmd/weight/data/cache 四类段 + `MCU_TO_DEV` 地址转换 |
| 用 feats.txt 定 Q 值 | ✅ **量化校准数据集（calibration）**，`Q = floor(log2(32767/max_abs))` |
| 排除 printf 计时 | ✅ **基准测试方法论（benchmark 排除 IO 开销）** |
| 流式 3 帧打包 | ✅ **chunk 化推理 / 多帧打包（L=3）** |

## XX时期：可能做过但没提的（需核对）

| 术语 | 场景 |
|---|---|
| 🧠 **内存池（Memory Pool）** | 推理引擎反复申请/释放张量，用手工内存池避免碎片和 malloc 开销 |
| 🧠 **线程池（Thread Pool）** | KWS 的 OpenMP 背后就是线程池管理 |
| 🧠 **静态形状 vs 动态形状（Static/Dynamic Shape）** | 流式模型每帧形状固定=静态，可以预分配全搞定 |
| 🧠 **延迟隐藏（Latency Hiding）** | 音频帧流水：采一帧、算一帧、出一帧，三重缓冲 |
| 🧠 **功耗优化（Power-Performance Tradeoff）** | 降频/关核换功耗，板子上的现实约束 |
| 🧠 **RTF（Real-Time Factor）** | GX8006 上算的 0.044——这个词本身就可以写进简历 |
| 🧠 **端到端延迟 vs 吞吐（Latency vs Throughput）** | benchmark 时必然区分的两个指标 |

## 八、课程学习线：编译器术语

| 学习线 | 术语 |
|---|---|
| ONNX Runtime | ✅ **自定义算子、Session 内省、图优化控制、C API、I/O Binding（零拷贝）** |
| TVM | ✅ **Tensor Expression（TE）、Schedule（split/reorder/vectorize/parallel）、Relay IR、TensorIR、TVMScript、AutoTVM（搜索空间/测量）、MetaSchedule、TOPI、Graph Executor、交叉编译、BYOC（自带 codegen）** |
| MNN | ✅ **算子注册、三段式（转换/优化/执行）、PluginScale 插件算子、PTQ 量化** |
| MLIR | ✅ **Dialect、Pass、SSA/Block/Region、Bufferization、memref、linalg tiling/pack、Transform Dialect、Conversion Pass、ODS/TableGen** |

## 九、经验 ↔ MLIR/TVM 的映射（面试可以直接说）

```
你手工的"循环内 if 消除"     → loop peeling / versioning
你手工的"DMA pingpong"      → software pipelining
你手工的"宏+脚本翻译 asm"   → conversion pass
你手工的"FRAM/WRAM 规划"    → bufferization + memory space
你手工的"切 NNMAC 块"       → linalg tiling / multi-level tiling
你手工的"bank 布局偏置"     → pack / 数据布局变换（layout hint）
```

## 十、完整技术版图

```
硬件直觉层（T41）        搬运优化层（T41）        部署层（XX）         编译器层（学习线）
├─ NPU ISA（NNMAC/LD/LU） ├─ 双缓冲 pingpong       ├─ 模型移植（ORT/QNN）   ├─ TVM（IR/调度/codegen）
├─ bank conflict          ├─ 可编程 DMA（描述符）   ├─ 量化（fp16/int8/定点） ├─ MNN（算子/注册/量化）
├─ tiling / 分块          ├─ DMA 压缩              ├─ 流式推理（状态回灌）   ├─ MLIR（dialect/pass）
├─ 量化/反量化            ├─ stride-2 kernel 拆分  ├─ 性能分析（RTF/耗时）   └─ 内存规划→bufferization
├─ 地址计算优化           ├─ DW 权重重排           └─ 交叉编译（NDK）
└─ RAW hazard / 分支发散  └─ rollback / full
```

## 十一、简历一句话亮点（把口语换成术语）

- "基于描述符链的可编程 DMA 实现卷积数据搬运，配合 pingpong 双缓冲隐藏搬运延迟"
- "通过 stride-2 卷积 kernel 拆分 + DW 权重重排，将 MAC 利用率从 X% 提升到 Y%"（X/Y 补真实数字）
- "采用 DMA 压缩搬运稀疏权重，在有限片上带宽下提升有效带宽"
- "在 T41 上手工完成编译器该做的所有事（指令生成、内存规划、调度优化）"
- "ONNX → QNN → Hexagon NPU 全链路部署，fp16 量化，流式状态回灌，RTF 0.044"
- "开发的开放词唤醒（KWS）工程登上 **2026 世界人工智能大会（WAIC）**，获央视直播展示"
