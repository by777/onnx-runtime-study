# 技术名词表（个人技术栈梳理）

> **梳理范围**：T41 NPU 算子开发 → 推理部署（QNN / MNN / GX8006）→ 编译器学习线（ORT / TVM / MNN / MLIR）
>
> 🗂 **导航**
>
> | 章 | 内容 |
> |---|---|
> | [一](#一总览t41-npu-后端的七个决策轴) | 总览：七个决策轴（推荐先读） |
> | [二](#二存储层次轴-1) | 存储层次（轴 1） |
> | [三](#三数据搬运轴-2--4--6--7) | 数据搬运（轴 2 / 4 / 6 / 7） |
> | [四](#四计算侧nnmac-shape轴-3) | 计算侧 NNMAC shape（轴 3） |
> | [五](#五描述符与算子-lowering轴-5) | 描述符与算子 lowering（轴 5） |
> | [六](#六调度层级指令级--循环级--图级) | 调度层级 |
> | [七](#七其他优化技术) | 其他优化技术 |
> | [八](#八待核对清单) | 待核对清单 |
> | [九](#九现在推理部署层) | 现在：推理部署层 |
> | [十](#十课程学习线编译器术语) | 课程学习线 |
> | [十一](#十一经验--编译器映射面试可直接说) | 经验 ↔ 编译器映射 |
> | [十二](#十二完整技术版图) | 完整技术版图 |
> | [十三](#十三性能指标速查) | 性能指标速查 |
> | [十四](#十四简历亮点把口语换成术语) | 简历亮点 |
>
> 📌 **术语规范**：`ORAM`、`NNLDF`、`NNMAC` 等是**厂商私有叫法**，业界无统一学名；
> **面试一律使用正文里的正式术语**（关键处已标 ✅）。

---

## 一、总览：T41 NPU 后端的七个决策轴

### 1.1 七个决策轴

| 轴 | 你当时的叫法 | 正式术语 | 编译器对应 | 取值 | 详见 |
|---|---|---|---|---|---|
| 1 | DRAM / ORAM / FRAM / WRAM / BSRAM | **存储层次 + 内存空间分配（memory space）** | `bufferization` + `memref` memory space | 3 档 5 名 | §2 |
| 2 | CPU 访存 / DMA 搬运 / NNLDF | **访存通路下降（cost-model driven lowering）** | `memref.copy` → async / vector | 3 | §3.1 |
| 3 | 7 种 NNMAC shape | **Tile shape selection（指令选择）** | `linalg` tiling + `vectorize` | 7 | §4 |
| 4 | full / pingpong / rolling | **软件流水（software pipelining）+ 缓冲分配** | `scf.for` + async pipeline | 3 | §3.2 |
| 5 | ~100 个描述符寄存器 | **算子描述符（Operator Descriptor）** | 算子 lowering 的输出 | — | §5 |
| 6 | 压缩格式（FMP / raw / 0…） | **per-buffer 数据编码属性** | layout / encoding attribute | 3+ | §3.5 |
| 7 | DMA 配寄存器 / 自维护地址跳转 | **控制面卸载（微码）+ 硬件地址生成（AGU）** | `scf.for` → target op lowering | — | §3.3 §3.4 |

> **这七条不是七个零散技巧，而是一个 DSP/NPU 编译器后端 7 个必备阶段在硬件上的体现。**

### 1.2 离散决策空间

$$3_{\text{存储档}} \times 3_{\text{搬运通路}} \times 7_{\text{shape}} \times 3_{\text{缓冲策略}} = 315\ \text{种组合}$$

> 硬件把每个维度**离散化**了：不给连续可调参数，只给有限枚举值。编译器（或当年的我）要在这些枚举的笛卡尔积里搜。

### 1.3 面试总纲（可直接背）

> T41 的 NPU 后端是一台**离散化的编译器** —— 存储层级离散成几档、搬运通路离散成 3 条、tile 形状离散成 7 种、缓冲策略离散成 3 种。我的工作，就是在这个离散空间里为每个算子手工找到一组可行组合。
> **AI 编译器要做的，就是把这个搜索自动化：cost model 替代经验，autotuning 替代试错。**

---

## 二、存储层次（轴 1）

### 2.1 三层结构（3 档 5 名）

```
Tier 1  片外     DRAM（主存：全模型 / 图像 / 中间结果）
              ↕ DMA（描述符链 / 自维护地址跳转）
Tier 2  片上·通用  ORAM（Scratchpad：通用暂存，兼作指令流存储）
              ↕
Tier 3  片上·专用  FRAM feature | WRAM weight | BSRAM bias·scale
              ↕ NNLDF 指令 load / CPU 访存
        ~100 个描述符寄存器（经 VR31 拆分通道填充）
              ↓
             NNMAC
```

| 档 | 名称 | 存什么 | 正式学名 |
|---|---|---|---|
| 片外 | **DRAM** | 全模型、图像、中间结果 | off-chip DRAM / 主存 |
| 片上通用 | **ORAM** | 通用暂存，**兼作指令流存储** | **Scratchpad SRAM (SPM)** → §2.4 |
| 片上专用 | **FRAM** | feature / 激活 | 角色化分区的 SPM |
| 片上专用 | **WRAM** | weight | 同上 |
| 片上专用 | **BSRAM** | bias / **scale** | 同上 |

> **兄弟芯片款**：另一款把第三块专用 SRAM 做成 **BTRAM**，存 bias / **threshold**（BTRAM 与 BSRAM 是两款不同芯片）。

**关键认知**：T41 的片上存储不是 cache（硬件自动管），是 **scratchpad（软件全管）**。
这个区别决定了**编译器必须承担全部布局责任** —— 我当年手工做的内存规划，就是 MLIR 里 `bufferization` + 内存空间分配的自动化目标。

### 2.2 为什么按数据角色分物理 SRAM

一个 MAC 单元要**同时**取 3 个操作数：activation（FRAM）、weight（WRAM）、bias/scale（BSRAM）。

| 方案 | 取数时间 | 面积 / 功耗 |
|---|---|---|
| 合并成**一块** RAM | 要么串行取（3 cycle），要么造三读端口 | 多端口 RAM 面积随端口数约按 $P^2$ 超线性增长 |
| 分**三块**单端口 RAM | 并行取，1 cycle | 3 × 单端口 ≈ 线性增长 |

$$\Rightarrow\ \text{"3 块单端口"比"1 块三读端口"便宜得多}$$

> **结论（推断）**：按角色分 RAM 不是"软件约定"，而是**物理事实**。
> **编译器含义**：memory space 是**硬件硬约束**，memory space assignment 必须尊重它。
> 这正是 MLIR 给 `memref` 打 memory space 属性、TVM 区分 `shared` / `local` scope 的硬件根因。

### 2.3 BSRAM vs BTRAM：用存储角色反推测算覆盖率

| 芯片变体 | 第三块专用 SRAM | 存什么 | 反推出的硬件能力 |
|---|---|---|---|
| 本款 | **BSRAM** | bias / **scale** | 仿射变换 $y = x\cdot s + b$ → **BN 折叠 + 定点反量化（requantize）** |
| 兄弟款 | **BTRAM** | bias / **threshold** | 带阈值激活 / 比较 → **硬件融合激活（clamp / threshold）** |

> **面试可讲**：看一块 NPU 的**片上存储角色划分**，就能反推它的**算子覆盖率** ——
> BSRAM 存 scale 说明硬件上做定点反量化（BN 已折叠进 scale/bias）；BTRAM 存 threshold 说明激活是硬件融合的。
> **存储资源怎么分，等于厂商用硅片声明了它支持哪些算子。**

**对"量化"这条短板的直接价值**：定点 MAC 输出必须乘 scale 还原 → **requantize 是硬件必经路径**，硬件为此专门留了一块存储。
这也解释了**为什么 BN 必须折叠**、**为什么通道要补齐** —— 不是数学便利，是硬件只给你两条路。

### 2.4 ORAM 的学名：Scratchpad Memory (SPM)

**判据**：指针可寻址 ✅ + 软件决定装什么 ✅ + 片上 ✅ + 访问时间确定 ✅ → **符合 Scratchpad 定义**。

| 判据 | Cache | ORAM（= Scratchpad） |
|---|---|---|
| 访问方式 | 透明（与 DRAM 同址） | **指针显式寻址** |
| 装什么 | 硬件按 miss 自动填充 / 驱逐 | **软件决定** |
| 访问时间 | 命中 / 缺失**不确定** | **确定（deterministic）** |
| tag 存储 | 有（占面积 / 功耗） | **无** → 同面积容量更大 |
| 冲突处理 | 硬件处理 | **软件处理**（bank 冲突就是软件责任） |

> **最关键一句**：**"能用指针访问"这件事本身就是它"不是 cache"的证明** —— 你永远不会"用指针访问 cache"，cache 是隐式的、看不见的。

**学名对照（按体系选）**

| 体系 | 正式名字 |
|---|---|
| 通用体系结构 | **Scratchpad Memory (SPM) / Software-managed SRAM** ← 首选 |
| ARM | **TCM（Tightly-Coupled Memory）**（ITCM / DTCM） |
| STM32 | **CCM（Core-Coupled Memory）** |
| GPU / OpenCL | **Local / Shared Memory** |
| 通用简称 | **On-chip SRAM / OCM** |

**三条澄清**

1. **"片上耦合内存"对应的是 TCM** —— 强调**紧耦合到 CPU 流水线、单周期访问、不经总线仲裁**。
   若该 RAM 走总线、能被 DMA 访问，就**不该叫 TCM**。
2. **⚠️ 缩写撞名**：`ORAM` 在计算机安全领域 = **Oblivious RAM（不经意随机访问存储器，抗侧信道攻击）**，与此**毫无关系**。
   面试若遇体系结构 / 安全背景的人可能被反问 → **一律说 Scratchpad**。
3. **全称不猜**：业界无 "ORAM = 片上 RAM" 的统一学名，是厂商私有缩写；但**类别是确定的**：片上、可寻址、软件管理的 SRAM。

> **面试表述**："T41 的 ORAM 是一块**片上 scratchpad SRAM**（软件管理、指针寻址、访问确定性），**兼任指令存储器** —— 我们写的微码就存在这里，DMA 读它来配寄存器。"

---

## 三、数据搬运（轴 2 / 4 / 6 / 7）

### 3.1 三条搬运通路（轴 2）

| 通路 | 粒度 | 地址生成成本 | 能否异步 / 重叠 | 启动开销 | 编译器对应 |
|---|---|---|---|---|---|
| **CPU 访存** | 标量 ~ 小向量 | 每笔都要算 | ❌ 阻塞核 | 极低 | `memref.load` / `store` 循环 |
| **DMA 搬运** | KB ~ MB，多维 | 描述符里带 stride | ✅ 异步 | **高** | `memref.copy` → `dma.start` / `wait`（async dialect） |
| **NNLDF 指令 load** | tile 级，**加载进 FRAM**（读地址另配） | 用寄存器里的地址 | 部分 | 中 | 向量化 copy / `linalg.copy` |

> **这是整份文档里编译器价值最高的一条**：同一个 `copy(src → dst)`，在 IR 里有**三条 lowering 路径**，
> 选哪条取决于 tile 大小 / 是否规则 / 上游是否已激活 DMA → **cost model 驱动的访存下降**，
> 也正是 BYOC（自带后端）要解决的核心问题。

**三条通路可以同时跑**：

```
DMA 搬第 k+2 块  ‖  NNLDF 喂第 k+1 块给 MAC  ‖  CPU 算第 k 块的边界 / 非规则索引
```

这已经不是二级 pingpong，而是 **三引擎并行（multi-engine overlap）**。
三条独立指令流 → 需要同步原语（barrier / event / fence）→ 对应 MLIR `async` dialect 的 `async.execute` + `async.await`。

### 3.2 三种搬运策略（轴 4）：full / pingpong / rolling

| 策略 | 缓冲划分 | 搬运粒度 | 片上占用 | 重叠 |
|---|---|---|---|---|
| **full** | 整块当 1 块用 | **一次搬完**整个 tile | 1× | ❌ 零重叠 |
| **pingpong** | 对半 2 块交替 | 半块 | 2× | ✅ 完全 |
| **rolling** | 多槽**环形**窗口滑动 | **增量（只搬新增）** | ≈窗口（最省） | ✅ 完全 + 数据复用 |

$$T_{full} = n\,(t_{load} + t_{compute}) \qquad\qquad T_{pingpong} \approx t_{load} + n\max(t_{load},\,t_{compute})$$

- **full** = 无流水的 naive 版，搬运时间**完整暴露**
- **pingpong** = **software pipelining（2 级）**，多出的 $t_{load}$ 是**流水线填充（prologue）**
- **rolling** = **环形缓冲（circular buffer）+ 行缓冲（line buffer）**，靠复用率 $\rho$ 压低 $t_{load}$

> **三者本质是"片上空间 ↔ 控制复杂度 ↔ 时间"的三方权衡**，与编译器的 buffer 大小决策完全同构。

| 你的叫法 | 编译器 / 框架术语 |
|---|---|
| full | 无软件流水（单缓冲） |
| pingpong | **Double Buffering / Software Pipelining**，TVM `T.Pipelined(num_stages=2)` |
| rolling | **Circular Buffer / Modulo Scheduling + 缓冲轮转（modulo variable expansion）** |
| 三者统一看待 | **Software Pipeline Stage Count 参数化** |

> **注意**：`rolling` 与 `pingpong` 逻辑上不互斥（滚动缓冲也能做双缓冲）；硬件把它们做成互斥 mode，说明厂商做了取舍封装。

### 3.3 rolling 的硬件本质：模寻址（轴 7 的一半）

"走到末尾**跳回 tensor buffer 第一行**" = 地址**回绕**：

$$addr = base + \big((offset) \bmod L\big) \times stride \qquad (L = \text{缓冲长度})$$

| 你的做法 | 正式术语 |
|---|---|
| 缓冲按槽滑动 | **环形缓冲 / 循环缓冲（Circular / Ring Buffer）** |
| 末尾跳回第一行 | **模寻址（Modulo Addressing）/ 回绕寻址（Wrap-around Addressing）** |
| 卷积滑窗复用 | **行缓冲（Line Buffer）**，HLS 里即 `hls::LineBuffer` |
| 硬件做这件事的单元 | AGU 里的**模寄存器 / 基长寄存器（Modulo / Base-Length Register）**；TI C54x 的 `BK`、Blackfin circular addressing |

> **为什么必须硬件化**：软件做回绕 = 每一圈要 `if (i == L) i = 0;` —— **正是被优化掉的"循环内 if"**；
> 硬件模寻址 = **0 条指令**，纯地址递推。

**⚠️ 地址生成的能力边界（面试有价值的细节）**

DMA / AGU 可表达的地址模式 = **仿射 + 模回绕**：

$$addr = \underbrace{base + i\,s_i + j\,s_j}_{\text{affine 仿射}} \quad \text{或} \quad \underbrace{base + (n \bmod L)\times s}_{\text{circular 模回绕}}$$

- **含 `mod` 就已经不是仿射了** → MLIR 的 `affine` 方言**表达不了 modulo**，要表达环形缓冲得靠**显式 wrap 逻辑**或带 circular 属性的 memory space
- **超出这个集合的访问**（如 `A[idx[i]]` 索引表 gather）→ **必须回落 CPU 访存通路**
- 这条分界线 ≈ **polyhedral 编译模型（Polly）的可编译性边界**："CPU 访存 vs DMA"两条通路的分工，本质是"**非仿射 vs 仿射**"的分工

> 附带经验：**回绕点跨 bank → bank 冲突常在回绕那一拍冒出来**（行缓冲通常要多留 padding）。

### 3.4 可编程 DMA 与控制面卸载（轴 7）

**先纠正一个常见误判：T41 的 DMA 不是 copy engine，是一台"小 CPU"。**

| 你的说法 | 正式术语 |
|---|---|
| DMA 的汇编指令存 iram | **指令存储器（I-memory）** |
| 操作地址存 dram（DMA 私有） | **数据存储器（D-memory）** |
| 两者分开、DMA 自带 | **哈佛架构（Harvard Architecture）** |
| 整机 = CPU + NNA + 可编程 DMA | **多引擎异构 SoC（三个可编程 agent）** |

> ⚠️ **命名碰撞**：此 `dram` 是 **DMA 私有数据 RAM**，与系统 DRAM 同名不同物 → 表述必须加限定词（"DMA 内部的 dram"）。
> 现代对应物：SmartNIC 的 P4 引擎、Intel DSA、DPU。

**所以"DMA 配寄存器"的真正含义是：跑一段 DMA 程序** —— 把指令放在某个 ORAM 地址 + 告诉它指令数 + 它自己去配寄存器。

```
DMA ─┬─ Data Plane    : 搬 feature / weight / raw（描述符描述"搬哪块数据"）
     └─ Control Plane : 搬配置 / 微码（描述符描述"配哪些寄存器"）
```

描述符因此从 **Data Descriptor** 升级为 **Command Descriptor**。同一模式在四个世界的四个名字：

| 世界 | 名字 |
|---|---|
| MCU / DSP | **Memory-to-Peripheral DMA**（把外设寄存器当搬运目的地） |
| GPU | **Command Buffer / PM4 Command Stream**（AMD）· Pushbuffer（NVIDIA） |
| TPU | **Instruction Queue**（XLA 编译出 VLIW 指令流存内存，TPU 顺序取指）← 最贴切 |
| QNN | **Context Binary**（就是后来的 `003_fp16_ctx.bin`！） |

> 💡 **闭环**：T41 手工做的"搬指令流配寄存器"，在 QNN 里就叫 **context binary** ——
> **把控制流固化成可搬运的指令流，让 NPU 脱离 CPU 独立跑完整个模型。**

**三个收益**：
1. CPU 从 N 次寄存器写 → **1 次 DMA 启动**（批量化摊销）
2. CPU 配下一层时，NPU 算当前层（**配置与计算重叠**）
3. 描述符可预挂 → 硬件自动衔接，**零 CPU 介入**

### 3.5 压缩搬运：per-buffer 格式元数据（轴 6）

**核心认知：压缩格式是 per-buffer 属性，不是 per-direction 属性。**

同一块 DDR 里可以同时存在不同格式的 buffer：

```
├─ 输入 raw 图像 buffer   → format = raw / 不压缩（枚举值 0）
├─ 权重 buffer           → format = 权重压缩（离线压好）
└─ 中间 feature buffer    → format = feature 压缩（NPU 写回时压）
```

DMA 描述符携带格式标签，硬件按标签决定 **编码 / 解码 / 直通**。
→ 原始图像标签为 raw → **直通** → 整条推理链路走通 ✅

**三种压缩对应三类冗余（信息论视角）**

| 数据 | 冗余类型 | 手段 | 正式术语 |
|---|---|---|---|
| **RAW 图像** | 空间冗余（相邻像素强相关） | 差分 / 预测 + 变长编码 | **DPCM 预测编码 + 熵编码**（"梯度差值"即此） |
| **Feature** | 结构冗余（量化后大量零） | 零值游程 / 稀疏标记 | **RLE / Zero-Skip / 稀疏编码** |
| **DW 权重** | 统计冗余（通道独立、值域小） | 位打包 / 码本 | **bit-packing / 矢量量化（VQ）** |

**为什么压缩能改写 Roofline**

$$I = \frac{\text{FLOPs}}{\text{Bytes}} \;\uparrow\; (\text{分母变小}) \;\Rightarrow\; \text{把算子从 memory bound 往 compute bound 挪}$$

> **面试一句**："压缩搬运 = **用计算换带宽**，本质是重定义 Roofline 的 **Bytes 轴**。"
> 现代对应物：2:4 结构化稀疏 Tensor Core、量化通信、KV cache 量化。

**与量化的同构（直接服务"量化理论"短板）**

$$\text{压缩}:\begin{cases}\text{硬件做编解码（算法固定）}\\[2pt]\text{工具链选格式 + 离线生成压缩数据}\end{cases} \quad\equiv\quad \text{量化}:\begin{cases}\text{硬件做定点 MAC（算术固定）}\\[2pt]\text{工具链选 scale + 离线生成定点参数}\end{cases}$$

> 早就在 T41 做过"**硬件算术固定、工具负责选参数 + 离线备数据**"这套模式 —— 只是当时叫压缩，现在叫量化。

**关键结论**

- ✅ 编解码在**硬件**里做 → **对计算数据通路透明** → **编译器不必考虑"解压融合（fusion）"**
- ✅ 压缩模式是**可寄存器配置的枚举项** → 属 per-tensor 元数据
- ⚠️ `FMP` 全称待补（记忆中有"图像类型 / feature"两种说法，见 §8）

### 3.6 搬运侧优化全家桶（一览）

```
pingpong（双缓冲）→ rolling（环形滑窗）→ 可编程 DMA（描述符链）
        → 模寻址（回绕寻址）→ DMA 压缩（带宽倍增）→ 控制面卸载（微码）
        ┊ 正交机制：rollback（搬运失效回滚 / 容错，不是缓冲策略）
```

> **因果链（三个决策是耦合的，不是三个独立旋钮）**：
> ```
> rolling 策略（增量滑窗）→ 地址模式最复杂（非平凡递推）
>         ↓ 必须硬件化
> DMA 自维护地址跳转（AGU + 模寄存器）→ 省掉 CPU 端地址寄存器
>         ↓
> 描述符寄存器压力下降 → 能容纳更多 in-flight 数据
>         ↓
> 支撑更深流水（full → pingpong → rolling）
> ```
> 「搬运策略 × 地址生成 × 寄存器压力」是一个**成套的系统设计** —— 这是简历上最值钱的表达方式。

---

## 四、计算侧：NNMAC shape（轴 3）

### 4.1 前提：7 种 shape 的语义

**硬件固定 64 个 MAC 单元**，7 种 shape 是**同一批单元的 7 种几何排布**：

$$M \times N = 64,\quad M \in \{1,2,4,8,16,32,64\},\quad N = 64/M$$

**选择依据 = "MAC 阵列几何"与"输出 tile 几何"的匹配度。**

具体场景（原始回忆）：输出 tile 是 $8 \times 8$（64 个输出点）时，配 `8×8` 正好吃满；
配 `1×64` 时 M=1 只能覆盖 **1 行** → 要 **8 条指令**才覆盖 8 行，且列 lane 有 64 条但输出只有 8 列 → **56 条列 lane 空转** → **多跑 7 次** ✅

### 4.2 三条独立推理都指向 8×8

**路径 1 · 载入量（AM-GM 下界）**
每发一条 NNMAC 需从片上搬入 $M+N$ 个操作数、产出 $MN = 64$ 个 MAC：

$$f(M) = \frac{M+N}{MN} = \frac{M}{64} + \frac{1}{M} \;\ge\; 2\sqrt{\frac{1}{64}} = 0.25 \qquad (\text{等号当 } M = N = 8)$$

**路径 2 · lane 利用率**
$K = \lceil R/M \rceil \cdot \lceil C/N \rceil$（指令数），$\eta = \dfrac{R\cdot C}{64K}$

**路径 3 · 供数压力**
欲维持 II = 1（每 cycle 发一条），每 cycle 需供 $P \ge M + N$ 个操作数

| shape | 载入量 $M{+}N$ | 载入效率 $64/(M{+}N)$ | 指令数 $K$ | 利用率 $\eta$ | 供数需求 $P$ |
|---|---|---|---|---|---|
| 1×64 | 65 | 0.98 | 8 | 12.5% | 65 |
| 2×32 | 34 | 1.88 | 4 | 25% | 34 |
| 4×16 | 20 | 3.20 | 2 | 50% | 20 |
| **8×8** | **16** ← 最优 | **4.00** ← 最优 | **1** | **100%** ← 最优 | **16** ← 最优 |
| 16×4 | 20 | 3.20 | 2 | 50% | 20 |
| 32×2 | 34 | 1.88 | 4 | 25% | 34 |
| 64×1 | 65 | 0.98 | 8 | 12.5% | 65 |

> **三条独立推理（通信效率 / 计算效率 / 供数压力）都指向 8×8** ——
> 不是巧合：8×8 同时是"**通信最省**""**匹配最紧**""**供数最轻**"的形状。
> 这与 Tensor Core 取近方形（`16×8` / `16×16`）、HLS 偏好方形 tile 是同一道理。

### 4.3 三条推论

**① 无绝对最优，最优随输出形状移动**
输出若为 $4 \times 16$，则 `4×16` 才 100%，`8×8` 掉到 50%。
→ **硬件给 7 个离散点、编译器选** = 典型的 **HW-SW co-design**。

**② 这个决策可解析求解，不需要 autotuning**
候选只有 7 个、目标函数有闭式解 → 穷举 7 次即得最优。
*(对比：TVM 里 tile size 搜索空间连续且大，只能测量搜索。)*
→ **面试表述**："这类决策属于 **cost model 能精确覆盖**的部分，不该丢给 autotuning。"

**③ 严格解释了"通道补齐到 4/8/16 倍数"为什么必须做**
真实模型 $C_{out}$ 常是 12 / 24 / 36（移动端），不整除 8 → lane 必然空转。
→ **补齐不是"对齐美观"，是"消除 lane 空转"** —— 这是 PTQ 通道补齐（MNN `PluginScale` 那套）的**硬件根因**。

### 4.4 跨芯片差异与 target 参数化

- "有同时做 **32 个输出点**的，也有 **64 个输出点**的" → 上方 $MN=64$ 的表**只对 64-MAC 款成立**
- 各档 SRAM 容量 / 带宽"**不同芯片都不一样**"

$$\Rightarrow \text{同一软件栈需适配 MAC 阵列规模不同的多款芯片} \;\Rightarrow\; \textbf{编译器必须把 target 参数化}$$

这就是 TVM `target` / MLIR `TargetAttr` 存在的理由 —— T41 开发中亲身遇到过这个需求。

---

## 五、描述符与算子 lowering（轴 5）

### 5.1 核心等价关系

> **手工填 ~100 个配置寄存器 ≡ lowering 一个算子。**

那 ~100 个寄存器**不是通用数据寄存器**（不是寄存器分配的产物），
而是 **NNA 的配置 / 描述符寄存器**：pad 相关、bt 读地址、MAC shape、压缩模式、搬运策略……

| 你手工填的字段 | 编译器对应 |
|---|---|
| pad 相关寄存器 | conv 的 padding 属性 |
| bt 读地址寄存器 | operand 的 buffer 绑定 |
| MAC shape 字段 | tile size 属性 |
| 压缩模式字段 | 数据编码属性 |
| 搬运策略字段 | 流水级数属性 |

### 5.2 VR31 配置通道（512-bit 拆 16×32-bit）

| 你的做法 | 正式术语 |
|---|---|
| VR31（512-bit）拆成 16 个 32-bit 小寄存器 | **子寄存器 / 分 lane 视图（sub-register / lane view）** |
| 先往 lane 写数，再整条写进 NNA 配置寄存器 | **寄存器暂存 + 批量下发（register staging / packed write）** |
| 配置寄存器只能从向量寄存器写 | **专用配置通路（dedicated config path）** |

> **设计动机**：若给 100 个配置寄存器各配一条标量写入通路，CPU 侧标量寄存器会爆炸。
> → 改用一个宽向量寄存器当"**打包信封**"，一次写 16 个字段。
> **代价**：512 bit 里真正用到的字段很少（"太浪费"）—— 用空间换配置通路的简洁。

### 5.3 为什么 shape 早期必须是编译期常量

```
shape 决定微码"条数"（8×8 → 1 条；1×64 → 8 条）
   ↓ 指令流长度变了 → 必须重新 codegen
shape 在"编译器固定"时代是编译期常量
   ↓ 预研版改为"离线生成 N 个 bin，运行期挑一个"
```

| 术语 | 对应 |
|---|---|
| **compile-time specialization** | 8×8 硬编码进指令流 |
| **multi-versioning / fat binary** | 预研版的"多 bin"；CUDA fatbin |
| **runtime dispatch / tactic selection** | TensorRT tactic、cuDNN heuristic |
| **per-arch blob** | QNN 的 context binary（`003_fp16_ctx.bin`） |

> **面试一句**："T41 早期 shape 是编译期常量（因为 shape 决定微码条数），后来预研版改成运行期多 bin 派发 ——
> 这正是从 **static specialization** 走向 **runtime dispatch**，与 CUDA fatbin、TensorRT tactic selection 是同一演进方向。"

### 5.4 两条写算子路径 = 人肉编译器

| 你的做法 | 编译器里的对应 | 本质 |
|---|---|---|
| 宏 + 脚本翻译成 asm | ✅ **Codegen（代码生成器）** | 你的脚本 = 一个手写的 IR → 汇编 lowering pass |
| intrinsics 直接编译 | ✅ **编译器 intrinsics 层** | 编译器帮你做寄存器分配 / 指令调度，但 tiling、双缓冲还要自己来 |
| 宏 ≈ 高级指令，不关心寄存器 | ✅ **中间表示（IR）** | 一句话"高级指令"，底层细节由后端填充 |
| 手动调 MAC 利用率 | ✅ **Roofline 分析 / 峰值利用率** | 验证计算单元有没有吃饱 |
| 工具链用了 mips / riscv | ✅ **公开指令集 + 自研脚本前端** | 宏 → 汇编（厂商自研脚本）→ `.o`（as）→ 链接（ld） |

---

## 六、调度层级（指令级 / 循环级 / 图级）

### 6.1 三级粒度

```
指令级   寄存器错开 / 填延迟                ← 局部、经验性
   ↓
循环级   full / pingpong / rolling          ← 护城河（见 §3.2）
   ↓
图级     算子执行顺序 / 融合                ← 另一条线
```

### 6.2 指令级：做过什么、没做什么

| 你的做法 | 正式术语 |
|---|---|
| 避免"写后立即读" | **RAW 停顿规避（stall avoidance）** → **延迟隐藏 / 延迟槽填充（latency hiding / delay slot filling）** |
| 相同寄存器错开 | **反依赖 / 输出依赖消解（anti-dependency removal）**（编译器用**寄存器重命名**自动做，这里是手工做） |
| 简单交错排指令 | **基本块内局部列表调度（local list scheduling）** |
| **不**手写 nop 插空 | 功能正确性由**硬件互锁（interlock）**或工具链保证 → **只做性能优化** |

> **准确定性**：`correctness by hardware, performance by scheduling`
> —— **不排也能跑对，排了才快**，说明这是**优化导向的真实调度**（不是补齐功能）。
>
> ⚠️ **边界**：做的是"**填延迟 + 拉开寄存器依赖**"这个局部范围，
> **没有**做完整列表调度（没排 nop、没算 II）→ 简历表述应说"**局部指令调度**"而非"完整指令调度"。

### 6.3 为什么必须手工排

T41 NPU 是**静态调度架构（VLIW 风格）** —— 没有乱序执行（OoO）+ 寄存器重命名硬件，所以编译器 / 程序员必须排。

> 这就是为什么 DSP 领域有"**延迟槽**"这个专有概念，而 x86 程序员从不需要关心。

---

## 七、其他优化技术

| 你当时叫它 | 正式术语 | 一句话解释 |
|---|---|---|
| 写 `NNMAC / LD / LU` 宏 | ✅ **NPU ISA 编程** | 直接在指令集层面写算子，宏是汇编助记符的封装 |
| `LD(VR0, 0, ptr, 0)` | ✅ **向量寄存器 + 地址寻址** | Load 指令：目标向量寄存器、偏移、基址、步长 |
| 访问同一 bank 变慢 | ✅ **Bank Conflict（bank 冲突）** | 多个访问落在同一 bank 被硬件串行化，本该并行 |
| 读写互相等 | ✅ **RAW Hazard（读后写冲突）** | 后一条指令读的数据前一条刚写，流水线必须停顿（另有 WAR / WAW） |
| 循环里嵌套不必要的 if | ✅ **分支发散（Divergence）+ 循环不变代码外提（LICM）** | SIMD / MAC 阵列锁步执行，if 让部分 lane 空转 |
| 索引反复重算 | ✅ **公共子表达式消除（CSE）+ 地址计算优化** | 每次循环重算 `i*stride+offset`，应提出循环 |
| 数据放不下，切块算 | ✅ **Loop Tiling（循环分块）** | 切成 NNMAC 吃得了的小块，分多次算 |
| 手工规划搬运节奏 | ✅ **内存层次（Memory Hierarchy）管理** | DDR → 片上 → 寄存器的数据流规划 |
| 切 NNMAC 能吃的块 | ✅ **数据复用（Data Reuse）+ tiling** | 减少重复搬运，让数据在片上多算几次 |
| **DW 权重压缩** | ✅ **Depthwise 权重重排 / 权重打包（weight repacking）** | DW 卷积权重通道独立、尺寸极小，按 MAC 阵列组织方式重排打包（4×4 分块、通道补齐），提高搬运与计算利用率 |
| **stride=2 时 kernel 拆分** | ✅ **空间下采样分解 / 输入分裂（kernel splitting for stride-2）** | stride=2 卷积等价于把输入按奇偶坐标拆成 4 个子图，各做 stride=1 卷积再拼 —— 拆完都是**标准稠密计算**，能吃到 MAC 阵列满利用率 |
| **softmax 查表：3bit 表 + 5bit 插值** | ✅ **分段线性 LUT 近似 + 幂分解（exponent decomposition）** | exp 无 FPU 算不起 → 换底成 `2^x` 只查 $[0,1)$：整数部分用移位还原 $2^n$；小数部分 8bit 定点，高 3bit 定 8 段、低 5bit 做 32 级线性插值，只存 9 个端点表项，全程整数乘加无分支 |
| 量化 / 反量化 | ✅ **INT8 / FP16 量化与反量化** | NPU 只有整数 MAC；`scale` 存于 BSRAM（见 §2.3） |

---

## 八、待核对清单

> 以下为**尚未确认**的项，不做推测填充（避免把猜测当事实写进简历）。

| # | 项 | 状态 |
|---|---|---|
| 1 | 压缩模式具体名称与压缩比（`FMP` / raw-DPCM / 0） | ⚠️ `FMP` 全称待补；记忆中有"**图像类型**（压缩率较低）"与"**feature 特征**"两种说法，未合并 |
| 2 | 各档 SRAM 容量 / 带宽（DRAM·ORAM·FRAM·WRAM·BSRAM·BTRAM） | ⚠️ 待补（"不同芯片都不一样"） |
| 3 | NNMAC 的 latency 与 throughput（II） | ⚠️ 待补（硬件同时有这两个数） |
| 4 | MAC 阵列规模 | ⚠️ 已知**跨款不同**（32 / 64 输出点）→ 编译器需参数化 target（见 §4.4） |
| 5 | 下列"大概率做过但未确认"项 | 🧠 见下表 |

**🧠 推测做过但未确认**

| 类别 | 术语 | 可能场景 |
|---|---|---|
| 计算侧 | 循环展开（Loop Unrolling） | 手工把内层循环展开，减少循环开销、增加 ILP |
| | 权重布局变换（Weight Layout Transform） | NHWC→NCHW、通道重排，让 DMA 搬运连续、MAC 读起来顺 |
| | im2col / 直接卷积 | 卷积要么展开成矩阵乘（吃内存），要么手写直接卷积（省内存费功夫） |
| | Pad 与边界处理（Padding / Edge Handling） | 卷积边界补零，跟"循环内 if"是一对难兄难弟 |
| | SIMD 向量化（Vectorization） | 向量寄存器一次算多个元素 —— `LD(VR0,...)` 里的 VR 就是干这个的 |
| 存储侧 | 内存对齐（Memory Alignment） | 地址按 16/32 字节对齐，DMA 才高效、bank 才不串行 |
| | 片上存储复用（Buffer Reuse） | 前一层输出占的空间，后一层直接复用 —— 手工做的 bufferization |
| | 混合精度（Mixed Precision） | INT8 vs FP16 vs FP32 的取舍 |
| 调度侧 | 内核融合（Kernel Fusion） | Conv+BN+ReLU 合成一个算子，少一次片上↔DDR 往返 |
| | 多核 / 多通道并行（Parallelism） | 多个 MAC 阵列或通道同时算，摊分同步开销 |

---

## 九、现在：推理部署层

| 场景 | 术语 |
|---|---|
| ECNR：ONNX → QNN 编译 → Hexagon NPU | ✅ **QNN 工具链 / Hexagon HTP（v73）**，`003_fp16_ctx.bin` = fp16 量化模型 + **context binary**（见 §3.4）|
| ECNR 的 14 个 cache 状态 | ✅ **状态流式模型（streaming）/ 自回归状态回灌** |
| KWS：ARM Cortex-A7 优化 | ✅ **NEON 向量化 + OpenMP 并行**（group_conv2d / Gemm 并行化） |
| 开放词唤醒（aivox_ovkws） | ✅ **开放词 KWS 工程**：登上 **2026 世界人工智能大会（WAIC）**，获央视直播展示 |
| 量化到定点 | ✅ **定标（scale → multiplier + shift）**，避免浮点计算 |
| fast20k on GX8006 SNPU | ✅ **SNPU 任务封装**：`model.h` 的 cmd / weight / data / cache 四类段 + `MCU_TO_DEV` 地址转换 |
| 用 feats.txt 定 Q 值 | ✅ **量化校准数据集（calibration）**，$Q = \lfloor \log_2(32767/\max\vert abs\vert) \rfloor$ |
| 排除 printf 计时 | ✅ **基准测试方法论（benchmark 排除 IO 开销）** |
| 流式 3 帧打包 | ✅ **chunk 化推理 / 多帧打包（L=3）** |

**🧠 可能做过但未确认**

| 术语 | 场景 |
|---|---|
| 内存池（Memory Pool） | 推理引擎反复申请 / 释放张量，用手工内存池避免碎片和 malloc 开销 |
| 线程池（Thread Pool） | KWS 的 OpenMP 背后就是线程池管理 |
| 静态形状 vs 动态形状（Static / Dynamic Shape） | 流式模型每帧形状固定 = 静态，可预分配 |
| 延迟隐藏（Latency Hiding） | 音频帧流水：采一帧、算一帧、出一帧，三重缓冲 |
| 功耗优化（Power-Performance Tradeoff） | 降频 / 关核换功耗，板子上的现实约束 |
| RTF（Real-Time Factor） | GX8006 上算的 **0.044** —— 公式见 §13.2 |
| 端到端延迟 vs 吞吐 | benchmark 时必然区分；Little's Law 与 Roofline 判断见 §13 |

---

## 十、课程学习线：编译器术语

| 学习线 | 术语 |
|---|---|
| ONNX Runtime | ✅ **自定义算子、Session 内省、图优化控制、C API、I/O Binding（零拷贝）** |
| TVM | ✅ **Tensor Expression（TE）、Schedule（split / reorder / vectorize / parallel）、Relay IR、TensorIR、TVMScript、AutoTVM（搜索空间 / 测量）、MetaSchedule、TOPI、Graph Executor、交叉编译、BYOC（自带 codegen）** |
| MNN | ✅ **算子注册、三段式（转换 / 优化 / 执行）、PluginScale 插件算子、PTQ 量化** |
| MLIR | ✅ **Dialect、Pass、SSA / Block / Region、Bufferization、memref、linalg tiling / pack、Transform Dialect、Conversion Pass、ODS / TableGen** |

---

## 十一、经验 ↔ 编译器映射（面试可直接说）

```
T41 手工活                        →  编译器术语
──────────────────────────────────────────────────────────────
"循环内 if 消除"                  →  loop peeling / versioning
"DMA pingpong"                   →  software pipelining
"rolling 环形滑动 + 回绕跳回"      →  circular buffer / modulo scheduling
"宏+脚本翻译 asm"                 →  conversion pass
"FRAM/WRAM 规划"                 →  bufferization + memory space
"切 NNMAC 块（7 种 shape）"       →  linalg tiling / multi-level tiling
"bank 布局偏置"                   →  pack / 布局变换（layout hint）
"配 100 个描述符寄存器"            →  算子 lowering（lowering a op to target ops）
"DMA 描述符链 / 自维护地址"        →  scf.for → target op lowering + AGU
"寄存器错开 / 填延迟"              →  局部指令调度（local list scheduling）
"压缩格式选择"                     →  encoding / layout attribute（per-buffer）
```

---

## 十二、完整技术版图

```
硬件直觉层（T41）            搬运优化层（T41）            部署层（现在）              编译器层（学习线）
├─ NPU ISA（NNMAC/LD/LU）    ├─ 三档缓冲 full/pingpong/rolling  ├─ 模型移植（ORT/QNN/MNN）  ├─ TVM（IR/调度/codegen）
├─ 存储层次 3档5名            ├─ 模寻址（AGU 回绕）             ├─ 量化（fp16/int8/定点）    ├─ MNN（算子/注册/量化）
├─ bank conflict             ├─ 可编程 DMA（描述符链）          ├─ 流式推理（状态回灌）      ├─ MLIR（dialect/pass）
├─ tiling / 7 种 MAC shape   ├─ 控制面卸载（微码/context bin）   ├─ 性能分析（RTF/耗时）      └─ 内存规划 → bufferization
├─ 地址计算优化               ├─ DMA 压缩（per-buffer 格式）      └─ 交叉编译（NDK）
├─ RAW hazard / 分支发散      ├─ stride-2 kernel 拆分
└─ 描述符寄存器配置            └─ DW 权重重排
```

---

## 十三、性能指标速查

> 这一节是面试里"你到底会不会**量化地**描述性能"的分水岭。每条尽量配**自己项目里的锚点数字**。
> 参考：Learn Inference 第 2.4 章（Roofline）、H100 数据手册数字。

### 13.1 算力指标（Compute）

| 术语 | 正式定义 | 一句话 / 公式 |
|---|---|---|
| **MAC** | Multiply-Accumulate，乘累加 | 一次 MAC = 1 乘 + 1 加 = **2 OPS**。NPU 算力本质就是"数 MAC 阵列" |
| **OPS / FLOPs** | 操作数 / 浮点操作数 | ⚠️ 单位带 `/s` 是**算力**，不带是**工作量**，面试别混 |
| **TOPS** | Tera OPS $= 10^{12}$ OPS/s | 边缘 NPU 主流口径（INT8 峰值） |
| **TFLOPS** | Tera FLOPs $= 10^{12}$ FLOPs/s | 数据中心 GPU 口径（FP16 / BF16 / FP8） |
| **Peak Compute** | 理论峰值算力 | $P_{peak} = f \times N_{MAC} \times 2$（×2 因为乘法 + 加法各算一次） |
| **Achieved Compute** | 实测算力 | 由耗时反推，永远 < Peak |
| **TOPS/W** | 每瓦算力（能效） | **边缘芯片第一指标**，TOPS 大不代表强 |
| **TOPS/mm²** | 面积效率 | 芯片设计侧指标 |

$$\text{MAC 利用率} = \frac{\text{FLOPs}_{理论}}{\text{Peak TOPS} \times 10^{12} \times t_{实测}}$$

> 例：某 conv 层 8M MAC = 16M FLOPs，实测 0.5 ms，峰值按 0.5 TOPS
> → 利用率 $= 16\times10^6 / (0.5\times10^{12}\times 0.5\times10^{-3}) = 6.4\%$。
> **低利用率不是坏事，它是简历上"MAC 利用率从 X% 提到 Y%"那句话的来源。**
> 也直接关联 §4.2 的 $\eta = RC/(64K)$ —— **同一个量，一个从算子维度算，一个从指令维度算。**

### 13.2 延迟与吞吐（Latency / Throughput）

| 术语 | 定义 | 备注 |
|---|---|---|
| **Latency（延迟）** | 单个请求从进到出的时间 | 用户体感指标 |
| **Throughput（吞吐）** | 单位时间完成的请求数 | 成本指标（FPS / tokens/s / QPS） |
| **Little's Law** | $\text{并发数} = \text{吞吐} \times \text{延迟}$ | 反推要多少并发才够 |
| **Batch Size** | 一次前向打包多少样本 | 提升吞吐；单请求延迟略增（不是降） |
| **TTFT** | Time To First Token | LLM **prefill** 阶段，决定"感觉快不快" |
| **TPOT / ITL** | Time Per Output Token | LLM **decode** 阶段，决定"吐字快不快" |
| **RTF** | Real-Time Factor $= t_{处理} / t_{音频}$ | **锚点：GX8006 上 RTF ≈ 0.044**（处理 1 s 音频只用 44 ms） |
| **Goodput** | 满足 SLA 前提下的有效吞吐 | 尾部延迟炸了，吞吐数字再高也没用 |
| **P50 / P95 / P99** | 延迟分位数 | 均值骗人，尾部才是体验 |

> **两阶段两纪律**（Learn Inference 第 0 章）：LLM **prefill 是算力瓶颈**（大矩阵乘、算术强度高），
> **decode 是带宽瓶颈**（每吐一个 token 都要重读全部权重）→ batching 救 decode，kernel 优化救 prefill。

### 13.3 带宽与 Roofline（判断瓶颈在哪）

| 术语 | 定义 | 公式 |
|---|---|---|
| **Bandwidth（带宽）** | 每秒能搬多少字节 | GB/s。⚠️ DDR / DMA / 片上 SRAM 三条带宽要分开算 |
| **算术强度 $I$** | 每搬 1 字节做多少次运算 | $I = \dfrac{\text{FLOPs}}{\text{Bytes}}$ |
| **机器平衡点 $I_{ridge}$** | 设备算力与带宽之比 | $I_{ridge} = \dfrac{\text{Peak FLOPs/s}}{\text{Bandwidth B/s}}$，单位 ops/byte |
| **Memory Bound** | $I < I_{ridge}$ | 加算力没用 → **减少搬运 / 提高数据复用** |
| **Compute Bound** | $I > I_{ridge}$ | 加带宽没用 → **降精度 / 换更快的 kernel** |
| **有效带宽** | 实测可达带宽 | 观察到**带宽饱和（bandwidth saturation）**时的触顶值 |
| **带宽饱和** | 有效带宽 < 需求带宽 | 即"搬运跟不上计算"，NPU 等数据 |

**锚点数字（可直接背）**：
- H100 FP16：$989\ \text{TFLOPS} \div 3.35\ \text{TB/s} \approx \mathbf{295\ ops/byte}$（平衡点）
- LLM **prefill**：算力密集 → **compute bound**
- LLM **decode @ batch=1**：$I \approx 2 \ll 295$ → 铁定 **memory bound**
- 70B 模型 FP16 权重 = 140 GB，H100 读一遍 ≈ **42 ms**，算力再快也白搭

> **套到 T41 场景**：**带宽饱和** = 有效带宽 < 需求带宽；
> "切 NNMAC 能吃的块" = **提高 $I$**（同一块数据在片上多算几次 ≡ 减少 Bytes）。
> **根治办法永远是提高算术强度，不是堆带宽。** 而 DMA 压缩（§3.5）正是**从分母下手**的手段。

### 13.4 流水线与 cycle 级指标

| 术语 | 定义 | 对应 |
|---|---|---|
| **Cycle（时钟周期）** | 最小时间单位，$t_{cycle} = 1/f$ | 数 MAC 阵列空转了几个周期 |
| **CPI / IPC** | 每指令周期数 / 每周期指令数 | IPC ↑ = 并行度高 |
| **Pipeline 深度** | 流水线级数 | NPU 的 LD → MAC → ST 三级 |
| **II（Initiation Interval）** | 相邻两次迭代的启动间隔 | **稳态吞吐 = 1/II** |
| **Fill / Drain** | 流水线填充 / 排空 | 循环边界的头尾开销 |
| **Bubble（气泡）** | 流水线里的空转 | 分支发散、bank conflict 都产生气泡 |
| **Stall / Hazard** | 流水线停顿 / 冒险 | **RAW = "读写互相等"**（另有 WAR / WAW） |
| **Startup Latency** | 流水线启动延迟 | $\approx \text{深度} \times t_{cycle}$，短循环最吃亏 |
| **Software Pipelining** | 软件流水 | ✅ **pingpong 双缓冲就是它** |
| **Latency Hiding** | 延迟隐藏 | 搬运被计算掩盖，前提：搬运时间 < 计算时间 |
| **Overlap** | 计算 / 搬运重叠率 | 重叠失败 = 流水线气泡 |
| **Amdahl 定律** | 加速比上限 | $S = \dfrac{1}{(1-p) + p/s}$，串行部分决定天花板 |
| **稳态吞吐 vs 启动延迟** | 两个不同的数 | "多久出一个"vs"多久出第一个"，别混 |

> **一句话串起来**：流水线**深度**决定"第一个结果要等多久"，**II** 决定"之后每隔多久出一个"，
> 双缓冲（软件流水）就是把"搬"塞进"算"的空隙里做 **latency hiding**。

### 13.5 能效与利用率

| 术语 | 公式 | 备注 |
|---|---|---|
| **MAC 利用率** | 实测算力 / 峰值算力 | 见 §13.1 |
| **带宽利用率** | 实测带宽 / 峰值带宽 | 接近 100% = 带宽饱和 |
| **能效** | TOPS/W | 边缘芯片核心竞争指标 |
| **面积效率** | TOPS/mm² | 芯片设计侧 |
| **TDP / 功耗** | W | 板子降频的根因 |
| **Speedup** | 优化前耗时 / 优化后耗时 | 简历直接写这个 |
| **Overhead** | 非计算耗时占比 | benchmark 必须扣除（排除 printf 就是这个） |

### 13.6 速查公式卡

$$\text{Peak TOPS} = \frac{f \times N_{MAC} \times 2}{10^{12}}$$

$$\text{MAC 利用率} = \frac{\text{FLOPs}}{\text{Peak TOPS} \times 10^{12} \times t}$$

$$I = \frac{\text{FLOPs}}{\text{Bytes}}, \qquad I_{ridge} = \frac{\text{Peak FLOPs/s}}{\text{Bandwidth}}$$

$$\eta_{lane} = \frac{R \cdot C}{64 \cdot K}, \qquad K = \left\lceil \frac{R}{M} \right\rceil \left\lceil \frac{C}{N} \right\rceil$$

$$\text{吞吐} = \frac{\text{并发数}}{\text{延迟}}, \qquad \text{RTF} = \frac{t_{process}}{t_{audio}}$$

> **判断流程**：算 $I$ → 比 $I_{ridge}$ → **低于平衡点就优化搬运**，高于平衡点才去优化 kernel。
> 这一步做完，就从"经验驱动调优"升级到"**模型驱动调优**"了。

---

## 十四、简历亮点（把口语换成术语）

### 14.1 可直接用的句子

- "在 NPU 上以 **ISA 级编程**（NNMAC / LD / ST 宏 + 脚本 codegen）实现卷积等算子，**手工完成编译器该做的指令生成、内存规划与调度**"
- "基于**描述符链的可编程 DMA** 实现卷积数据搬运，配合 **pingpong 双缓冲 / rolling 环形缓冲**隐藏搬运延迟"
- "手工规划**三档片上存储（feature / weight / bias-scale 分区）**的地址布局，规避 **bank 冲突**与 **RAW hazard**"
- "通过 **stride-2 卷积 kernel 拆分** + **DW 权重重排**，将 **MAC 利用率从 X% 提升到 Y%**"（X/Y 补真实数字）
- "采用 **DMA 压缩搬运**（per-buffer 格式：raw / DPCM / 稀疏）在有限片上网带宽下**提升有效带宽**"
- "按 **7 种 MAC 阵列 shape** 匹配输出 tile 几何，将 lane 空转降到最低（`8×8` 配置下 **MAC lane 利用率 100%**）"
- "以 **Roofline 模型**定位瓶颈：算术强度低于机器平衡点时优先优化搬运，高于时优化 kernel"
- "**ONNX → QNN → Hexagon NPU** 全链路部署，fp16 量化，**流式状态回灌**，**RTF 0.044**"
- "ARM Cortex-A7 上以 **NEON 向量化 + OpenMP 并行**优化 group_conv2d / Gemm，降低 CPU 占用率"
- "开发的**开放词唤醒（KWS）工程登上 2026 世界人工智能大会（WAIC）**，获央视直播展示"

### 14.2 加分表达（体现"双端视角"）

> "T41 的经历让我形成了硬件侧直觉（MAC 利用率、bank 冲突、DMA 双缓冲）**和**编译器侧方法（IR、调度、pass、codegen）——
> 当年手工做的 tiling / 内存规划 / 流水线调度，正是现在 MLIR 里 `linalg tiling`、`bufferization`、pipelining pass 要自动化的事。
> 所以我看编译器 pass 时，能立刻对应到硬件上真实发生的代价。"

### 14.3 待补真实数字（诚实列短板）

| 项 | 状态 |
|---|---|
| T41 MAC 利用率具体数字（优化前 / 后） | ⚠️ 待补 |
| KWS CPU 占用率下降幅度 | ⚠️ 待补 |
| 毕业时间 / 各段经历起止 | ⚠️ 待补 |
| 量化理论（per-channel / per-tensor 误差分析） | 短板，正在补（Lesson 21） |
| 编译器源码级理解（读 pass 源码 + C++ 写 pass） | 短板，正在补（MLIR） |
| 性能建模（roofline / 计算强度） | §13 已系统化 |
